Liste des services exposés par l'API. Chaque service a sa propre page de documentation avec son contrat d'interface (body, result), ses exemples et ses limites.

{services_table}

> Tous ces services sont consommables via la route générique `POST /v1/services/{service}/tasks` (voir section **Tasks**). Le nom du service à utiliser dans le path est indiqué dans la doc de chaque service.

Pour voir l'ordre de mise à disposition des prochains services, consultez la [**Feuille de route**]({roadmap_anchor}).
